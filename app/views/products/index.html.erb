<h1>Products</h1>

<div class="products-grid">
  <% @products.each do |product| %>
    <div class="product-card">
      <h2><%= product.title %></h2>
      <p>$<%= product.price %></p>
    </div>
  <% end %>
</div>
